# Non-terminal performance investigation: sidebar, git refresh, persistence, and Resource Manager

Generated by `pnpm exec vitest run --config config/vitest.non-terminal-perf.config.ts`.

## Scope

This investigation covers non-terminal items from `NON_TERMINAL_PERFORMANCE_BENCHMARK_IDEAS.md` and intentionally avoids Terminal/PTY implementation files. Browser/webview lifecycle work is also intentionally skipped because another owner is working on that area. These are local opt-in benchmark samples, not CI pass/fail budgets.

## Item 2: sidebar/worktree-list row projection

Measured the pure row-building path used by `WorktreeList`: `buildRows(...)`, plus host-section projection where relevant. This does not claim full React commit time, but it isolates the deterministic CPU work that feeds the virtualized sidebar rows.

| Scenario | Worktrees | Output rows | Timing |
| --- | ---: | ---: | ---: |
| repo grouping, 10 repos × 100 worktrees | 1000 | 1061 | 0.39 ms median / 3.39 ms p95 / 0.69 ms mean (100 iterations) |
| status grouping, 10 repos × 100 worktrees | 1000 | 1053 | 0.44 ms median / 1.57 ms p95 / 0.71 ms mean (100 iterations) |
| PR grouping, 10 repos × 100 worktrees | 1000 | 1052 | 1.66 ms median / 2.59 ms p95 / 1.87 ms mean (100 iterations) |
| host sections, 10 repos × 100 worktrees × 3 hosts | 1000 | 1066 | 0.58 ms median / 2.54 ms p95 / 0.72 ms mean (100 iterations) |

### Item 2 findings

- The row projection does not look like a sustained bottleneck at 1,000 worktrees: medians were single-digit milliseconds in this sample, and the heavier grouping paths are the ones to watch in a full React/Electron profile.
- Active worktree switches should not rebuild `rows` because the main `useMemo(buildRows)` dependency list does not include `activeWorktreeId`; the active-card visual update is a separate path.
- The heavier measured path is PR grouping; host sections add another partitioning step but were not the slowest path in this sample.
- If users report sidebar lag at very large workspace counts, the next measurement should be a real React Profiler/Electron run for commit time and visible-row render counts. The pure projection is not obviously the bottleneck at 1,000 rows.

## Item 3: git/worktree refresh subprocess fanout

Measured actual `listWorktrees(repoPath)` calls against temporary git repositories with real linked worktrees. Git subprocess count is captured with a temporary PATH shim around `git`, while wall time is measured around the `listWorktrees` call itself.

| Scenario | Repos | Extra worktrees per repo | Returned worktrees | Git processes / iteration | Timing |
| --- | ---: | ---: | ---: | ---: | ---: |
| single repo refresh | 1 | 10 | 11 | 1 | 7.21 ms median / 8.14 ms p95 / 7.44 ms mean (20 iterations) |
| single repo refresh | 1 | 30 | 31 | 1 | 10.19 ms median / 11.15 ms p95 / 10.29 ms mean (20 iterations) |
| single repo refresh | 1 | 60 | 61 | 1 | 14.44 ms median / 15.01 ms p95 / 14.43 ms mean (20 iterations) |
| concurrent all-repo refresh | 5 | 10 | 55 | 5 | 11.72 ms median / 13.52 ms p95 / 11.96 ms mean (20 iterations) |

### Item 3 refresh-frequency / fanout check

Code inspection and count modeling for the known worktree refresh callers. For local git repos, current `listWorktrees` measurement above shows one local git process per repo-scoped refresh; runtime-host rows are provider/RPC refreshes instead of local git processes.

| Scenario | Repo count | Repo-scoped refreshes | Local git processes if all local git repos | Lineage refreshes | Window | Source |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| idle visible app, no repo/runtime event | 10 | 0 | 0 | 0 | 60s | Code search found no fixed fetchAllWorktrees/listWorktrees idle interval. |
| App startup fetchAllWorktrees | 10 | 10 | 10 | 1 | event-driven | App.tsx startup hydration calls fetchAllWorktrees(), then fetchWorktreeLineage(). |
| Sidebar repo-count effect after repo list changes | 10 | 10 | 10 | 0 | event-driven | Sidebar effect runs fetchAllWorktrees() when repos.length changes. |
| Startup plus sidebar first repo-count effect | 10 | 20 | 20 | 1 | event-driven | Normal startup can plausibly hit the App startup path plus the Sidebar repo-count effect. |
| worktrees:changed for one repo | 10 | 1 | 1 | 1 | event-driven | useIpcEvents handleWorktreesChanged(repoId) refreshes the touched repo, then lineage. |
| runtime reposChanged / remote connect for one host | 10 | 10 | 0 | 1 | event-driven | Runtime-host paths call provider listWorktrees once per repo, not local git. |
| Automations page mount | 10 | 10 | 10 | 0 | event-driven | AutomationsPage mount calls fetchAllWorktrees() before refreshing automations. |
| idle visible app, no repo/runtime event | 50 | 0 | 0 | 0 | 60s | Code search found no fixed fetchAllWorktrees/listWorktrees idle interval. |
| App startup fetchAllWorktrees | 50 | 50 | 50 | 1 | event-driven | App.tsx startup hydration calls fetchAllWorktrees(), then fetchWorktreeLineage(). |
| Sidebar repo-count effect after repo list changes | 50 | 50 | 50 | 0 | event-driven | Sidebar effect runs fetchAllWorktrees() when repos.length changes. |
| Startup plus sidebar first repo-count effect | 50 | 100 | 100 | 1 | event-driven | Normal startup can plausibly hit the App startup path plus the Sidebar repo-count effect. |
| worktrees:changed for one repo | 50 | 1 | 1 | 1 | event-driven | useIpcEvents handleWorktreesChanged(repoId) refreshes the touched repo, then lineage. |
| runtime reposChanged / remote connect for one host | 50 | 50 | 0 | 1 | event-driven | Runtime-host paths call provider listWorktrees once per repo, not local git. |
| Automations page mount | 50 | 50 | 50 | 0 | event-driven | AutomationsPage mount calls fetchAllWorktrees() before refreshing automations. |
| idle visible app, no repo/runtime event | 100 | 0 | 0 | 0 | 60s | Code search found no fixed fetchAllWorktrees/listWorktrees idle interval. |
| App startup fetchAllWorktrees | 100 | 100 | 100 | 1 | event-driven | App.tsx startup hydration calls fetchAllWorktrees(), then fetchWorktreeLineage(). |
| Sidebar repo-count effect after repo list changes | 100 | 100 | 100 | 0 | event-driven | Sidebar effect runs fetchAllWorktrees() when repos.length changes. |
| Startup plus sidebar first repo-count effect | 100 | 200 | 200 | 1 | event-driven | Normal startup can plausibly hit the App startup path plus the Sidebar repo-count effect. |
| worktrees:changed for one repo | 100 | 1 | 1 | 1 | event-driven | useIpcEvents handleWorktreesChanged(repoId) refreshes the touched repo, then lineage. |
| runtime reposChanged / remote connect for one host | 100 | 100 | 0 | 1 | event-driven | Runtime-host paths call provider listWorktrees once per repo, not local git. |
| Automations page mount | 100 | 100 | 100 | 0 | event-driven | AutomationsPage mount calls fetchAllWorktrees() before refreshing automations. |

### Item 3 findings

- Current `listWorktrees` uses one `git worktree list --porcelain -z` subprocess per local repo refresh on current git.
- Sparse-checkout annotation no longer adds N git subprocesses per worktree; it uses filesystem reads/stat through `resolveGitDir` + `info/sparse-checkout` checks.
- I did not find a fixed idle worktree-list poller. In a 60-second idle window with no repo/runtime events, expected worktree-list refreshes are 0.
- The remaining churn risk is burst fanout, especially startup: `App.tsx` startup and the Sidebar repo-count effect can plausibly produce two all-repo refresh waves. At 100 local repos, that is 200 repo-scoped refreshes / local git subprocesses across the two waves.
- The visible Source Control git-status poll is separate: it runs every 3s only when eligible/visible for the active worktree and uses `git status`, not `git worktree list`.

## Item 4: persistence and store payload bloat

Measured production session builders with a synthetic non-browser profile: 25 repos, 500 worktrees, 1,000 open editor files, and dirty editor drafts. Browser maps are intentionally empty in this benchmark.

### Patch builder payloads

| Scenario | Changed fields | Patch keys | Patch byte estimate | Build timing |
| --- | --- | --- | ---: | ---: |
| active worktree switch | activeWorktreeId | activeWorktreeId | 64 B | 0 ms median / 0 ms p95 / 0 ms mean (100 iterations) |
| editor focus change | activeFileIdByWorktree, activeTabTypeByWorktree | openFilesByWorktree, activeFileIdByWorktree, activeTabTypeByWorktree, markdownFrontmatterVisible | 384.28 KB | 0.39 ms median / 0.58 ms p95 / 0.43 ms mean (100 iterations) |
| dirty editor draft edit | editorDrafts | openFilesByWorktree, activeFileIdByWorktree, activeTabTypeByWorktree, markdownFrontmatterVisible | 384.28 KB | 0.34 ms median / 0.54 ms p95 / 0.38 ms mean (100 iterations) |
| unified editor tab order/layout change | unifiedTabsByWorktree | unifiedTabs, tabGroups, tabGroupLayouts, activeGroupIdByWorktree | 761.35 KB | 0.34 ms median / 0.49 ms p95 / 0.36 ms mean (100 iterations) |

### Full payload / disk-stringify shape

| Scenario | Repos | Worktrees | Open files | Dirty draft bytes | Workspace session JSON | Full persisted JSON | Payload build timing | Full stringify timing | Largest top-level fields |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| scaled editor/workspace state; browser maps intentionally empty | 25 | 500 | 1000 | 100 KB | 1.43 MB | 1.56 MB | 0.81 ms median / 1.03 ms p95 / 0.84 ms mean (30 iterations) | 1.56 ms median / 1.87 ms p95 / 1.6 ms mean (30 iterations) | workspaceSession 1.43 MB; worktreeMeta 44.78 KB; settings 7.56 KB; repos 3.88 KB; ui 2.4 KB |

### Subscriber gate checks

| Scenario | Updates | Persist calls | Elapsed | Notes |
| --- | ---: | ---: | ---: | --- |
| 200 unrelated store ticks after warmup | 200 | 0 | 5.99 ms | Only agentStatusEpoch changed; session-relevant references stayed stable. |
| 50 rapid editor-focus-shaped relevant updates | 50 | 1 | 8.38 ms | Debounce coalesced the burst; the emitted patch still uses the full editor-session shape. |

### Item 4 findings

- The session subscriber gate is doing the important cheap thing: unrelated store ticks did not schedule persistence after warmup.
- Small scalar updates stay tiny. An active-worktree switch only emits the active-worktree field.
- Editor-shaped relevant updates rebuild and send the full editor session slice. That is correct behavior today, but it means an editor focus or draft change pays for all currently-open persisted editor files, not just the one file that changed.
- The main persistence writer still stringifies the full persisted state when a debounced save fires. In this synthetic non-browser profile, `workspaceSession` is the largest top-level field, so payload growth matters even when the renderer sends a patch.

## Item 5: Resource Manager and workspace-space scans

Measured the Resource Manager workspace-space analyzer with folder repos. Folder repos isolate disk scanning from git worktree discovery, and the POSIX fast path counts `du -k -d 1` invocations with a temporary shim.

### Disk scan path

| Scenario | Repos | Worktrees | Top-level items | Raw analyzer progress events | du processes / iteration | Timing |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 10 folder repos, 8 top dirs + 4 root files each | 10 | 10 | 120 | 51 | 10 | 18.71 ms median / 19.95 ms p95 / 18.9 ms mean (3 iterations) |
| 30 folder repos, 8 top dirs + 4 root files each | 30 | 30 | 360 | 151 | 30 | 52.36 ms median / 58.5 ms p95 / 54.13 ms mean (3 iterations) |

### UI projection helpers

| Scenario | Source rows | Output rows | Git-status candidates | Timing |
| --- | ---: | ---: | ---: | ---: |
| filter query + sort by size | 1000 | 111 | 0 | 0.11 ms median / 0.13 ms p95 / 0.12 ms mean (100 iterations) |
| only deletable + sort by repo | 1000 | 960 | 0 | 0.16 ms median / 0.26 ms p95 / 0.17 ms mean (100 iterations) |
| git-status candidate selection | 1000 | 960 | 960 | 0.01 ms median / 0.02 ms p95 / 0.01 ms mean (100 iterations) |
| treemap layout for all visible rows | 1000 | 1000 | 0 | 0.13 ms median / 0.43 ms p95 / 0.2 ms mean (100 iterations) |
| filter query + sort by size | 5000 | 111 | 0 | 0.51 ms median / 0.67 ms p95 / 0.55 ms mean (100 iterations) |
| only deletable + sort by repo | 5000 | 4800 | 0 | 0.95 ms median / 1.11 ms p95 / 0.98 ms mean (100 iterations) |
| git-status candidate selection | 5000 | 4800 | 4800 | 0.04 ms median / 0.04 ms p95 / 0.04 ms mean (100 iterations) |
| treemap layout for all visible rows | 5000 | 5000 | 0 | 0.69 ms median / 0.98 ms p95 / 0.73 ms mean (100 iterations) |

### Delete-readiness editor-file counting shape

The current Resource Manager decision-details loop derives editor counts per row by filtering the full open-file list for each row. This benchmark mirrors that shape and compares it with a pre-indexed map. It is a measurement of the algorithmic shape, not a product behavior change.

| Scenario | Rows | Open files | Current-shape timing | Indexed-shape timing |
| --- | ---: | ---: | ---: | ---: |
| Resource Manager delete-readiness editor-file counting shape | 1000 | 5000 | 30.94 ms median / 34.53 ms p95 / 29.04 ms mean (100 iterations) | 0.03 ms median / 0.08 ms p95 / 0.04 ms mean (100 iterations) |
| Resource Manager delete-readiness editor-file counting shape | 5000 | 25000 | 352.17 ms median / 454.25 ms p95 / 365.09 ms mean (100 iterations) | 0.21 ms median / 0.32 ms p95 / 0.23 ms mean (100 iterations) |

### Item 5 findings

- Opening the Resource Manager panel does not automatically run the disk scan; the scan is manual. Duplicate scan requests are deduped in both renderer and main, and cancel is wired through an AbortController.
- Local POSIX scans use one `du -k -d 1` subprocess per scanned local worktree. Folder repos map to one workspace each, so the 30-repo sample used 30 `du` processes per scan iteration.
- The pure filter/sort/treemap helpers scale acceptably in these synthetic samples, but the delete-readiness editor-file counting shape is a real UI-side scaling risk at thousands of rows/open files. If the panel janks after a scan result lands, pre-indexing editor/dirty counts by worktree is the first small fix to test.
- After scan rows exist, the panel auto-refreshes git status for deletable candidate rows with concurrency 6. That is not idle unless a scan result is present, but a large scan can trigger a follow-up git-status burst.

## Suggested next actions

1. Skip browser/webview benchmarking in this branch while that work has a separate owner.
2. For item 3, consider a small startup-only fix or benchmark: dedupe/coalesce the App startup `fetchAllWorktrees` wave and the Sidebar repo-count effect so startup cannot double-refresh every repo.
3. For item 4, if editor-heavy users report periodic stalls, prototype an editor-session patch that can update active editor IDs/drafts without rebuilding the entire open-file payload; keep a byte-size regression test at the patch boundary.
4. For item 5, if Resource Manager jank is reproduced, pre-index per-worktree editor counts before building decision details and benchmark panel commit time before touching disk-scan behavior.
5. Keep future product fixes separate from this benchmark harness so before/after numbers stay trustworthy.
